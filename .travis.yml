language: cpp
compiler: 
  - gcc

before_script:
  - sudo apt-get update -y
  - sudo apt-get install -y libboost-all-dev
  - ./GetLibsI.py gtest

script:
  # For now just cpp tests, retry
  - ./cpp/build.sh clean test
